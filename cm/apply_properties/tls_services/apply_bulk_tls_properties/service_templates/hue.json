{
   "services": [
        {
            "serviceType": "{{ service_type }}",
            "refName": "{{ service_ref_name }}",
            "roleConfigGroups": [
{{ role_config_groups }}
            ]
        }
    ]
}